version: '3.8'

services:
  # common_h5 服务
  common-h5:
    build:
      context: .
      dockerfile: apps/h5/common_h5/Dockerfile
      args:
        NODE_ENV: production
    image: common-h5:prod
    container_name: common-h5-prod
    ports:
      - "7998:7998"
    environment:
      - NODE_ENV=production
      - PORT=7998
      - HOSTNAME=0.0.0.0
    env_file:
      - ./apps/h5/common_h5/.env.production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:7998"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - app-network

  # common_web 服务
  common-web:
    build:
      context: .
      dockerfile: apps/web/common_web/Dockerfile
      args:
        NODE_ENV: production
    image: common-web:prod
    container_name: common-web-prod
    ports:
      - "7999:7999"
    environment:
      - NODE_ENV=production
      - PORT=7999
      - HOSTNAME=0.0.0.0
    env_file:
      - ./apps/web/common_web/.env.production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:7999"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
